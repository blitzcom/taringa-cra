version: 2

jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - restore_cache:
          key: v1-packages-{{ .Branch }}-{{ checksum "yarn.lock" }}
      - run:
          name: Install node packages
          command: |
            yarn install
      - save_cache:
          key: v1-packages-{{ .Brach }}-{{ checksum "yarn.lock" }}
          paths:
            - node_modules
